{
  "name": "granola-sync",
  "version": "1.0.0",
  "description": "Brutal simplification: Granola to Obsidian sync",
  "type": "module",
  "scripts": {
    "sync": "bun run sync.ts",
    "test": "bun tests/sync.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5.0.0"
  }
}
